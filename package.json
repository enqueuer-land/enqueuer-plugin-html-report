{
  "name": "enqueuer-plugin-html-report",
  "version": "0.0.1",
  "description": "Enqueuer html-report plugin",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "all": "npm run clean && npm run lint && npm run build && npm run updateIndex",
    "build": "tsc",
    "clean": "rm -rf dist",
    "updateIndex": "nqr conf.yml -l `pwd` && mv output.html index.html",
    "lint": "node_modules/.bin/tslint --project tsconfig.json",
    "prepublishOnly": "npm run all"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/lopidio/enqueuer-plugin-html-report.git"
  },
  "keywords": [
    "enqueuer",
    "nqr",
    "html",
    "report",
    "plugin"
  ],
  "author": "Virgs",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/lopidio/enqueuer-plugin-html-report/issues"
  },
  "homepage": "https://github.com/lopidio/enqueuer-plugin-html-report#readme",
  "devDependencies": {
    "@types/node": "^11.9.4",
    "ts-node": "^8.0.2",
    "tslint": "^5.11.0",
    "typescript": "^3.0.1"
  },
  "dependencies": {
    "enqueuer-plugins-template": "^1.0.5"
  }
}
